# Triage Status — 2026-09-03

> *This was generated by AI during triage.*

## Question
16 open GitHub tickets are all labeled `ready-for-agent`. Are they implemented (should be closed) or still pending (need implementation)?

## Method
For each ticket, checked the codebase for the concrete artifacts named in its acceptance criteria (routes, components, hooks, lib modules, tests, usages) rather than trusting the label.

## Verdict

| # | Ticket | Status | Evidence |
|---|--------|--------|----------|
| 28 | Ticket 1: Marketing route structure + layout | ✅ DONE | `src/app/(marketing)/layout.tsx` + `page.tsx` with Header/Footer |
| 29 | Ticket 2: Hero section | ✅ DONE | `src/components/marketing/Hero.tsx` |
| 30 | Ticket 3: Features section | ✅ DONE | `src/components/marketing/Features.tsx` |
| 31 | Ticket 4: How It Works | ✅ DONE | `src/components/marketing/HowItWorks.tsx` |
| 32 | Ticket 5: FAQ section | ✅ DONE | `src/components/marketing/FAQ.tsx` |
| 33 | Ticket 6: CTA + assembly | ✅ DONE | `src/components/marketing/CTA.tsx`; `page.tsx` assembles all 5 sections |
| 34 | Ticket 7: E2E + perf tests | ✅ DONE* | `src/e2e/homepage.spec.ts` — sections, nav, scroll, FAQ accordion, mobile 320/768/1024/1440, Core Web Vitals (LCP/CLS/FID) |
| 35 | Ticket 8: Analytics + SEO | ✅ DONE* | `layout.tsx` — Plausible `Script` (lazyOnload) + `PageViewTracker`/`ScrollTracker` + full metadata (title, OG, twitter, canonical) + Organization structured data |
| 19 | 16-01: useHousehold hook | ✅ DONE | `src/hooks/useHousehold.ts` |
| 20 | 16-02: home page uses useHousehold | ✅ DONE | `src/app/home/page.tsx` |
| 21 | 16-03: migrate remaining pages | ❌ NOT DONE | `categories/` & `to-buy/` still use inline `createClient`/`supabase.from(...)`; only `items` + `members` migrated (2 of 4) |
| 22 | 17-01: usePushManager hook | ✅ DONE | `src/hooks/usePushManager.ts` |
| 23 | 17-02: usePushSubscriptionSync | ✅ DONE | `src/hooks/usePushSubscriptionSync.ts` |
| 24 | 17-03: usePushNotifications wrapper | ✅ DONE | `src/hooks/usePushNotifications.ts` composes both hooks and coordinates request/subscribe/unsubscribe |
| 25 | 18-01: itemOperations module | ✅ DONE | `src/lib/itemOperations.ts` (createItem/updateItem/updateItemQuantity/deleteItem, all `{error}`) |
| 26 | 18-02: items page uses itemOperations | ✅ DONE | `src/app/items/page.tsx` imports & uses createItem/updateItem/updateItemQuantity/deleteItem |

\* Minor caveats: Lighthouse CI wired into the pipeline (not just the test file) and the homepage sitemap entry were not verified.

## Recommendation

- **Close 15 tickets as already-implemented** (#28–35, #19–20, #22–26). Per the triage skill, do NOT write to `.out-of-scope/` for these — they were built, not rejected. Each close comment should point to where the code lives.
- **Keep #21 open.** It is fully specified (good `ready-for-agent` candidate) but incomplete: the `categories` and `to-buy` pages still use inline Supabase queries and must be migrated to the `useHousehold` hook to satisfy its acceptance criteria.

## Note on the labels
All 16 were tagged `ready-for-agent`, but 15 were effectively "done and forgotten." That label is meant for fully-specified work awaiting an agent — not for finished work. Once closed, the only remaining open issue is #21.
